---
- name: Install packages using apt
  apt:
    pkg:
      - nmap
      - p7zip-full
      - p7zip-rar
      - vnstat
      - iftop
      - whois
      - ncdu
      - jq
      - lynx
      - zsh
      - tree
      - netdata
      - zip
      # used in alias'
      - pwgen
      # for traefik
      - apache2-utils
      # For gpg key creation on passbolt
      - rng-tools
    state: present
